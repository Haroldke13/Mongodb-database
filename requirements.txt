# GENERATED for deployment by reading every import in app.py.
# This repo had no requirements.txt.
#
#   flask          app.py (Flask, render_template, url_for, request, redirect)
#   pymongo        app.py (MongoClient, bson.objectid.ObjectId)
#   frozen-flask   app.py (Freezer) — imported at module scope, so it is a hard
#                  dependency even though freezing only runs under __main__
#   gunicorn       production WSGI server (added for deployment)
#
# google_auth.py is an empty file and contributes no dependencies.

Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.5
itsdangerous==2.2.0
click==8.1.8
MarkupSafe==3.0.2

pymongo==4.10.1
dnspython==2.7.0

Frozen-Flask==1.0.2
gunicorn==23.0.0
